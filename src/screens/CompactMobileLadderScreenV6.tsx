export { CompactMobileLadderScreenV5 as CompactMobileLadderScreenV6 } from './CompactMobileLadderScreenV5';

// V6 planning checkpoint:
// Stable base: V5.
// Next implementation target:
// - Add explicit Move mode for selected blocks.
// - Selected block becomes the move origin.
// - Existing + drop zones become destination zones.
// - Tapping a destination zone removes the block from origin and inserts it there.
// - Keep the compact dock unchanged.
// - Keep white native visual as the default theme.
